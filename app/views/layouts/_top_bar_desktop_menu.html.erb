<%# Keep synchronized with top_bar_mobile_menu so that we offer the same options on desktop and mobile. %>

<%= top_bar_desktop_link "Support", "https://github.com/flexpointtech/repeater_world/discussions" %>
<%= top_bar_desktop_link "Open Source", open_source_url %>
<%= top_bar_desktop_link "Log in", new_user_session_url if !user_signed_in? %>
<%= top_bar_desktop_link "Sign up", new_user_registration_url if !user_signed_in? %>
